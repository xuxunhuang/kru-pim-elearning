import { requireUser } from "@/lib/auth";import { assertAdmin } from "@/lib/data";import Link from "next/link";import { AttemptClient } from "./AttemptClient";import "./attempt.css";export const dynamic="force-dynamic";export default async function Page({params}:{params:Promise<{id:string}>}){const user=await requireUser();assertAdmin(user);const{id}=await params;return <main className="attempt-shell"><Link href="/admin">← กลับหลังบ้าน</Link><AttemptClient id={id}/></main>}
