import { env } from "cloudflare:workers";import { NextRequest,NextResponse } from "next/server";import { requireUser } from "@/lib/auth";import { driveFileId,getDriveAccessToken } from "@/lib/google-drive";
export const dynamic="force-dynamic";
export async function GET(request:NextRequest,{params}:{params:Promise<{lessonId:string}>}){const user=await requireUser();const{lessonId}=await params,now=new Date().toISOString();const lesson=await env.DB.prepare(`SELECT l.provider_asset_id asset FROM lessons l JOIN courses c ON c.id=l.course_id JOIN enrollments e ON e.course_id=c.id WHERE l.id=?1 AND e.user_id=?2 AND l.type='video' AND l.status='published' AND c.status='published' AND e.status='active' AND COALESCE((SELECT starts_at FROM access_overrides o WHERE o.enrollment_id=e.id AND o.lesson_id=l.id),e.starts_at)<=?3 AND COALESCE((SELECT ends_at FROM access_overrides o WHERE o.enrollment_id=e.id AND o.lesson_id=l.id),e.ends_at)>=?3 LIMIT 1`).bind(lessonId,user.id,now).first<{asset:string}>();if(!lesson||!lesson.asset.startsWith("drive:"))return NextResponse.json({error:"ไม่พบวิดีโอหรือไม่มีสิทธิ์"},{status:404});try{const token=await getDriveAccessToken();const headers=new Headers({authorization:`Bearer ${token}`});const range=request.headers.get("range");if(range)headers.set("range",range);const upstream=await fetch(`https://www.googleapis.com/drive/v3/files/${driveFileId(lesson.asset)}?alt=media`,{headers});if(!upstream.ok||!upstream.body)return NextResponse.json({error:upstream.status===403?"ไฟล์ Drive ยังไม่ได้แชร์ให้ระบบ":"เปิดวิดีโอไม่สำเร็จ"},{status:upstream.status});const out=new Headers({"content-type":upstream.headers.get("content-type")||"video/mp4","cache-control":"private, no-store","accept-ranges":"bytes","content-disposition":"inline"});for(const key of ["content-length","content-range"])if(upstream.headers.get(key))out.set(key,upstream.headers.get(key)!);return new Response(upstream.body,{status:upstream.status,headers:out})}catch(error){const message=error instanceof Error&&error.message==="DRIVE_NOT_CONFIGURED"?"ระบบ Google Drive ยังไม่ได้ตั้งค่า":"เปิดวิดีโอไม่สำเร็จ";return NextResponse.json({error:message},{status:503})}}
